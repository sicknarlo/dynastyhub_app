
import styled from 'styled-components'
import { Table } from 'antd';

export default styled(Table)`
.ant-table-body {
  overflow: scroll;
}
td {
  white-space: nowrap;
}
th {
  white-space: nowrap;
}
`
